# Repository naming check

Checked: 2026-06-16

Recommended repository name:

    EnterOcto

Recommended module repository, if the project is later split:

    EnterOcto-Trace

Findings:

- No exact public GitHub repository match was found for `EnterOcto`.
- No exact public GitHub repository match was found for `EnterOcto-Trace`.
- A GitHub account named `enteroctopus` exists and owns an empty repository named `dofleini`.
- A repository named `Enter-octo-enigma` also exists, but it is not an exact or meaningful product-name collision.
- GitHub repository names only need to be unique inside a specific user or organization account. Other owners can legally use the same repository slug.
- For branding and search visibility, consistently use `EnterOcto` with no hyphen.

Suggested GitHub metadata:

Repository:
    EnterOcto

Description:
    Open-source shadow AI agent detection, runtime forensics, and evidence response platform.

Topics:
    ai-security
    agent-security
    shadow-ai
    wazuh
    zeek
    falco
    tshark
    dfir
    threat-detection
    incident-response
